<%= render 'header' %>
<h3>以下の内容で、送信する。</h3>
<%= form_with(model: @voice, url: voices_path, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>発言:<%= @voice.content %></p>
  <%= form.submit  "登録する" %>
  <%= form.submit "戻る", name: 'back' %>
<% end %>
<%= render 'footer' %> 